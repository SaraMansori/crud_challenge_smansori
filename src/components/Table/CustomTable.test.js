import React from "react";
import 'testing-library/jest-dom/extend-expect'
import { render } from "@testing-library/react";

import CustomTable from "./CustomTable";

test('renders table', () => {
  const table = {

  }

  const view = render(<CustomTable />)
})